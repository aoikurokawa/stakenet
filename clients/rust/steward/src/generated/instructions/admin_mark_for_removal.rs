//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

/// Accounts.
pub struct AdminMarkForRemoval {
    pub config: solana_program::pubkey::Pubkey,

    pub state_account: solana_program::pubkey::Pubkey,

    pub authority: solana_program::pubkey::Pubkey,
}

impl AdminMarkForRemoval {
    pub fn instruction(
        &self,
        args: AdminMarkForRemovalInstructionArgs,
    ) -> solana_program::instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: AdminMarkForRemovalInstructionArgs,
        remaining_accounts: &[solana_program::instruction::AccountMeta],
    ) -> solana_program::instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.config,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.state_account,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            self.authority,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = AdminMarkForRemovalInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_program::instruction::Instruction {
            program_id: crate::STEWARD_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshDeserialize, BorshSerialize)]
pub struct AdminMarkForRemovalInstructionData {
    discriminator: [u8; 8],
}

impl AdminMarkForRemovalInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [213, 225, 98, 245, 9, 15, 154, 63],
        }
    }
}

impl Default for AdminMarkForRemovalInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct AdminMarkForRemovalInstructionArgs {
    pub validator_list_index: u64,
    pub mark_for_removal: u8,
    pub immediate: u8,
}

/// Instruction builder for `AdminMarkForRemoval`.
///
/// ### Accounts:
///
///   0. `[writable]` config
///   1. `[writable]` state_account
///   2. `[writable, signer]` authority
#[derive(Clone, Debug, Default)]
pub struct AdminMarkForRemovalBuilder {
    config: Option<solana_program::pubkey::Pubkey>,
    state_account: Option<solana_program::pubkey::Pubkey>,
    authority: Option<solana_program::pubkey::Pubkey>,
    validator_list_index: Option<u64>,
    mark_for_removal: Option<u8>,
    immediate: Option<u8>,
    __remaining_accounts: Vec<solana_program::instruction::AccountMeta>,
}

impl AdminMarkForRemovalBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn config(&mut self, config: solana_program::pubkey::Pubkey) -> &mut Self {
        self.config = Some(config);
        self
    }
    #[inline(always)]
    pub fn state_account(&mut self, state_account: solana_program::pubkey::Pubkey) -> &mut Self {
        self.state_account = Some(state_account);
        self
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_program::pubkey::Pubkey) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn validator_list_index(&mut self, validator_list_index: u64) -> &mut Self {
        self.validator_list_index = Some(validator_list_index);
        self
    }
    #[inline(always)]
    pub fn mark_for_removal(&mut self, mark_for_removal: u8) -> &mut Self {
        self.mark_for_removal = Some(mark_for_removal);
        self
    }
    #[inline(always)]
    pub fn immediate(&mut self, immediate: u8) -> &mut Self {
        self.immediate = Some(immediate);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: solana_program::instruction::AccountMeta,
    ) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_program::instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_program::instruction::Instruction {
        let accounts = AdminMarkForRemoval {
            config: self.config.expect("config is not set"),
            state_account: self.state_account.expect("state_account is not set"),
            authority: self.authority.expect("authority is not set"),
        };
        let args = AdminMarkForRemovalInstructionArgs {
            validator_list_index: self
                .validator_list_index
                .clone()
                .expect("validator_list_index is not set"),
            mark_for_removal: self
                .mark_for_removal
                .clone()
                .expect("mark_for_removal is not set"),
            immediate: self.immediate.clone().expect("immediate is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `admin_mark_for_removal` CPI accounts.
pub struct AdminMarkForRemovalCpiAccounts<'a, 'b> {
    pub config: &'b solana_program::account_info::AccountInfo<'a>,

    pub state_account: &'b solana_program::account_info::AccountInfo<'a>,

    pub authority: &'b solana_program::account_info::AccountInfo<'a>,
}

/// `admin_mark_for_removal` CPI instruction.
pub struct AdminMarkForRemovalCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_program::account_info::AccountInfo<'a>,

    pub config: &'b solana_program::account_info::AccountInfo<'a>,

    pub state_account: &'b solana_program::account_info::AccountInfo<'a>,

    pub authority: &'b solana_program::account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: AdminMarkForRemovalInstructionArgs,
}

impl<'a, 'b> AdminMarkForRemovalCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_program::account_info::AccountInfo<'a>,
        accounts: AdminMarkForRemovalCpiAccounts<'a, 'b>,
        args: AdminMarkForRemovalInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            config: accounts.config,
            state_account: accounts.state_account,
            authority: accounts.authority,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> solana_program::entrypoint::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.config.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.state_account.key,
            false,
        ));
        accounts.push(solana_program::instruction::AccountMeta::new(
            *self.authority.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_program::instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = AdminMarkForRemovalInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_program::instruction::Instruction {
            program_id: crate::STEWARD_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.config.clone());
        account_infos.push(self.state_account.clone());
        account_infos.push(self.authority.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_program::program::invoke(&instruction, &account_infos)
        } else {
            solana_program::program::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `AdminMarkForRemoval` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` config
///   1. `[writable]` state_account
///   2. `[writable, signer]` authority
#[derive(Clone, Debug)]
pub struct AdminMarkForRemovalCpiBuilder<'a, 'b> {
    instruction: Box<AdminMarkForRemovalCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> AdminMarkForRemovalCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_program::account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(AdminMarkForRemovalCpiBuilderInstruction {
            __program: program,
            config: None,
            state_account: None,
            authority: None,
            validator_list_index: None,
            mark_for_removal: None,
            immediate: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn config(
        &mut self,
        config: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    #[inline(always)]
    pub fn state_account(
        &mut self,
        state_account: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.state_account = Some(state_account);
        self
    }
    #[inline(always)]
    pub fn authority(
        &mut self,
        authority: &'b solana_program::account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn validator_list_index(&mut self, validator_list_index: u64) -> &mut Self {
        self.instruction.validator_list_index = Some(validator_list_index);
        self
    }
    #[inline(always)]
    pub fn mark_for_removal(&mut self, mark_for_removal: u8) -> &mut Self {
        self.instruction.mark_for_removal = Some(mark_for_removal);
        self
    }
    #[inline(always)]
    pub fn immediate(&mut self, immediate: u8) -> &mut Self {
        self.instruction.immediate = Some(immediate);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_program::account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(
            &'b solana_program::account_info::AccountInfo<'a>,
            bool,
            bool,
        )],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program::entrypoint::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(
        &self,
        signers_seeds: &[&[&[u8]]],
    ) -> solana_program::entrypoint::ProgramResult {
        let args = AdminMarkForRemovalInstructionArgs {
            validator_list_index: self
                .instruction
                .validator_list_index
                .clone()
                .expect("validator_list_index is not set"),
            mark_for_removal: self
                .instruction
                .mark_for_removal
                .clone()
                .expect("mark_for_removal is not set"),
            immediate: self
                .instruction
                .immediate
                .clone()
                .expect("immediate is not set"),
        };
        let instruction = AdminMarkForRemovalCpi {
            __program: self.instruction.__program,

            config: self.instruction.config.expect("config is not set"),

            state_account: self
                .instruction
                .state_account
                .expect("state_account is not set"),

            authority: self.instruction.authority.expect("authority is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct AdminMarkForRemovalCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_program::account_info::AccountInfo<'a>,
    config: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    state_account: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    authority: Option<&'b solana_program::account_info::AccountInfo<'a>>,
    validator_list_index: Option<u64>,
    mark_for_removal: Option<u8>,
    immediate: Option<u8>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(
        &'b solana_program::account_info::AccountInfo<'a>,
        bool,
        bool,
    )>,
}
